{
    "name": "catalyst-monorepo",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build:catalyst-core": "npm run prepare --workspace packages/catalyst-core",
        "lint": "npm run lint --workspaces --if-present",
        "lint-staged": "lint-staged",
        "sync-cca-templates": "node scripts/release/sync-cca-templates.js",
        "test": "sh ./scripts/test-catalyst-core.sh"
    },
    "devDependencies": {
        "husky": "^9.0.11",
        "lint-staged": "^15.2.2"
    }
}
